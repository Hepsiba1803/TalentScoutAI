# ignore python virtual enviroment (it has many files that are specific to the local machine and should not be committed to version control)
venv/

# python cache files(makes the code run faster not needed to be commited to version control
_pycache_/
*.pyc

# environment variables (api keys should not be shared in version control)
env/
.env

# files related to ide settings (unnecessary in version control)
.vscode/

# small files generated by os to speed up file access
.DS_Store
Thumbs.db

# Project Specific files (stores errors and logs not required in version control)
*.log